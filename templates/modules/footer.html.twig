{% set urlfmt = 'text-dark text-decoration-none' %}

<footer class="container mt-2 mt-md-3 d-print-none">

  <div class="row justify-content-around text-center pt-2 pt-md-3">
    {% for sponsor in base.sponsors %} 
      <div class="sponsor col col-4 col-md-3 col-lg-2" {% if sponsor.url is not empty %}onclick="window.open('{{sponsor.url}}','_blank');"{%endif%}>
        {% if sponsor.image is not empty %}
          <img src="{{ vich_uploader_asset(sponsor, 'imageFile') }}" class="mx-auto img-fluid" alt="{{sponsor.name}}">
        {% endif %}
        {{sponsor.caption}}
      </div>
    {% endfor %}
  </div><!-- close div row -->
</footer>

<footer class="bg-gold pb-4 pt-4 text-dark d-print-none">
<div class="container">
  <h4 class="text-hozt"># We are red and gold</h4>
  <div class="row">
    <div class="col-12 col-md pt-2 pt-md-3">
      <img class="mb-2" src="{{ asset('assets/img/logo/Hozt.svg') }}" alt="" height="40">
      <address class="text-red">
        <span>Hoogstratens Zwem Team vzw</span><br>
        <small>
          Achtelsestraat 76A, 2320 Hoogstraten<br>
          <abbr title="Bankrekening">B:</abbr> <span>BE88&nbsp;0689&nbsp;0449&nbsp;7341<br>
          <abbr title="Ondernemingsnummer">O:</abbr> BE0644.916.871
        </small>
      </address>
    </div>
    <div class="col-12 col-md pt-2 pt-md-3 text-center">
      <h4><i class="fas fa-swimmer"></i><br>Aanbod</h4>
      <ul class="list-unstyled">
      {% for category in base.training_team_categories %}
        <li><a class="{{urlfmt}}" href="{{ path('training_category', {'slug': category.slug}) }}">{{ category.name }}</a></li>
      {% endfor %}
      </ul>
    </div>
    <div class="col-12 col-md pt-2 pt-md-3 text-center">
      <h4><i class="fas fa-fish"></i><br>Praktisch</h4>
      <ul class="list-unstyled">
        <li><a class="{{urlfmt}}" href="/lid-worden">Lid worden</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('enrol_tryout') }}">Testmoment</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('training_schedule_teams') }}">Trainingsuren</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('calendar') }}">Kalender</a></li>
        <li><a class="{{urlfmt}}" href="/clubkledij">Clubkledij</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('blog') }}">Nieuwsberichten</a></li>
<!--    <li><a class="{{urlfmt}}" href="#">Clubrecords</a></li> -->
        <li><a class="{{urlfmt}}" href="{{ path('enrol_list') }}">Inschrijven</a></li>
        <li><a class="{{urlfmt}}" href="/interscholen">Interscholen Zwemwedstrijd</a></li>
      </ul>
    </div>
    <div class="col-12 col-md pt-2 pt-md-3 text-center">
      <h4><i class="fas fa-swimming-pool"></i><br>Algemeen</h4>
      <ul class="list-unstyled">
        <li><a class="{{urlfmt}}" href="{{ path('training_coaches') }}">Trainers</a></li>
        <li><a class="{{urlfmt}}" href="/bestuur">Bestuur</a></li>
        <li><a class="{{urlfmt}}" href="/vacatures">Vacatures</a></li>
        <li><a class="{{urlfmt}}" href="/zwembad">Zwembad</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('sponsors') }}">Sponsors</a></li>
        <li><a class="{{urlfmt}}" href="/gedragsregels">Gedragsregels</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('contact_faq') }}">Veelgestelde vragen</a></li>
        <li><a class="{{urlfmt}}" href="{{ path('contact_form') }}">Contact</a></li>
      </ul>
    </div>
  </div>
</div>
</footer>

<div class="bg-red d-print-none">
  <div class="nav-scroller container mt-0">
    <nav class="nav nav-underline">
      <a class="nav-link" href="javascript:;"><i class="fas fa-copyright"></i> 2016-{{ "now"|date("Y") }}</a>
      <a class="nav-link" href="/huishoudelijk-reglement">Huishoudelijk Reglement</a>
      <a class="nav-link" href="/privacy">Privacy</a>
      <a class="nav-link" href="https://www.steps4it.be" style="color: rgba(0,0,0,.5);"><i class="fas fa-code"></i> STEPS4it</a>
    </nav>
  </div>
</div>
