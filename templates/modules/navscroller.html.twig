<!-- NAV-SCROLLER
================================================== -->
<div class="nav-scroller bg-red shadow-sm fixed-top d-print-none" id="nav-categories">
  <nav class="nav nav-underline container">
  {% for category in base.training_team_categories %}
    <a class="nav-link" href="{{ path('training_category', {'slug': category.slug}) }}">{{ category.name }}</a>
  {% endfor %}
  </nav>
</div>
